{
    "name": "buckfifty-be",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "clean": "rm -rf dist",
        "build": "tsc",
        "start": "node dist/index.js",
        "server": "npm run clean && npm install && npm run build && npx prisma generate && npm run start",
        "dev": "DEV=1 ts-node-dev --respawn --transpile-only src/index.ts",
        "prisma:generate": "prisma generate",
        "prisma:migrate": "prisma migrate dev"
    },
    "dependencies": {
        "@prisma/client": "^6.16.2",
        "@types/uuid": "^10.0.0",
        "dotenv": "^17.2.2",
        "express": "^4.18.2",
        "openai": "^4.104.0",
        "twilio": "^5.10.4",
        "uuid": "^8.0.0",
        "winston": "^3.18.3"
    },
    "devDependencies": {
        "@types/express": "^5.0.3",
        "prisma": "^6.16.2",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.0.4"
    }
}
